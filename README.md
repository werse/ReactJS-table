### ReactJS + Spring boot application

To run application:  
_project requires installed gradle and nodeJs_

* __to run the server:__
```bash
gradle build
gradle bootRun
```
    
* __to compile and run the webpack in watch mode__
```bash
npm install
npm run dev
```